import React from 'react';
import ChatPage from './ChatPage';

// This is just a wrapper around ChatPage for now,
// but you could add different functionality in the future
const MessagesPage = () => {
  return <ChatPage />;
};

export default MessagesPage;